# الصفحات (Pages)

الصفحات تحدد المحتوى والويدجات لكل صفحة في المتجر.

## الموقع

```
pages/
├── index.json           # الصفحة الرئيسية
├── product.json         # صفحة المنتج الفردي
├── collection.json      # صفحة التصنيف
├── collections.json     # قائمة التصنيفات
├── cart.json            # سلة التسوق
├── wishlist.json        # المفضلة
├── search.json          # نتائج البحث
├── blog.json            # قائمة المدونة
├── blogs.json           # قائمة جميع المدونات
├── article.json         # صفحة مقال واحد
└── page.json            # صفحة عامة
```

---

## بنية ملف الصفحة

```json
{
  "layout": "layout",
  "title": "عنوان الصفحة",
  "widgets": [
    {
      "name": "hero-banner",
      "widget": "hero-banner",
      "settings": {
        "title": "مرحباً بكم"
      }
    },
    {
      "name": "featured-products",
      "widget": "product-grid",
      "settings": {
        "count": 8
      }
    }
  ]
}
```

---

## الخصائص

| الخاصية | النوع | الوصف |
|---------|------|-------|
| `layout` | نص | اسم ملف التخطيط من مجلد `layouts/` (بدون .njk) |
| `title` | نص | عنوان الصفحة للـ SEO والتنقل |
| `widgets` | مصفوفة | قائمة مرتبة من الويدجات |

---

## خصائص الويدجت في الصفحة

| الخاصية | النوع | مطلوب | الوصف |
|---------|------|-------|-------|
| `name` | نص | **نعم** | الاسم الذي يظهر في Theme Editor (محرر الثيم) |
| `widget` | نص | **نعم** | اسم مجلد الويدجت من `widgets/` |
| `settings` | كائن | لا | قيم إعدادات الويدجت |
| `blocks` | مصفوفة | لا | الـ Blocks إذا كان الويدجت يدعمها |

---

## بنية الـ Blocks في الصفحات

كل block يجب أن يحتوي على:

| الخاصية | النوع | مطلوب | الوصف |
|---------|------|-------|-------|
| `name` | نص | **نعم** | الاسم الذي يظهر في Theme Editor |
| `block` | نص | **نعم** | نوع الـ Block (يجب أن يطابق اسم الـ block في schema.json) |
| `settings` | كائن | لا | قيم إعدادات الـ Block |

```json
{
  "blocks": [
    {
      "name": "شريحة العروض",    // ← هذا ما يظهر في Theme Editor
      "block": "slide",
      "settings": {
        "title": "عنوان الشريحة"
      }
    },
    {
      "name": "شريحة المجموعة الجديدة",
      "block": "slide",
      "settings": {
        "title": "المجموعة الجديدة"
      }
    }
  ]
}
```

**مثال على ما يظهر في Theme Editor:**

```
📄 الصفحة الرئيسية
└── 🔲 سلايدر الرئيسية          ← name الويدجت
    ├── 📌 شريحة العروض         ← name الـ block
    └── 📌 شريحة المجموعة الجديدة  ← name الـ block
```

> ⚠️ **أخطاء شائعة**:
> ```json
> {
>   "type": "slide",    // ❌ خطأ - استخدم "block"
>   "block": "slide"    // ✅ صحيح
> }
> ```
> ```json
> {
>   "block": "slide",
>   // ❌ خطأ - نسيت إضافة "name"
> }
> ```

### خاصية `name` (مهم جداً)

خاصية `name` هي **الاسم الذي يظهر في محرر الثيم (Theme Editor)** للمستخدم. يجب أن يكون:

1. **فريداً** في كل صفحة - لا يمكن تكرار نفس الاسم مرتين في نفس الصفحة
2. **واضحاً ومفهوماً** - لأن المستخدم سيراه في لوحة التحكم
3. **باللغة العربية أو الإنجليزية** حسب جمهورك المستهدف

```json
{
  "widgets": [
    {
      "name": "سلايدر الصفحة الرئيسية",  // ← هذا ما يراه المستخدم في Theme Editor
      "widget": "hero-slider",
      "settings": { ... }
    },
    {
      "name": "المنتجات المميزة",  // ← اسم آخر واضح ومفهوم
      "widget": "featured-products",
      "settings": { ... }
    }
  ]
}
```

**مثال على ما يظهر في Theme Editor:**

```
📄 الصفحة الرئيسية
├── 🔲 سلايدر الصفحة الرئيسية     ← name
├── 🔲 التصنيفات                  ← name
├── 🔲 المنتجات المميزة           ← name
└── 🔲 آراء العملاء               ← name
```

> ⚠️ **تحذير**: إذا لم تضف `name` للويدجت، لن يظهر بشكل صحيح في Theme Editor وقد يسبب مشاكل في التعديل.

---

## أنواع الصفحات

### `index.json` - الصفحة الرئيسية

الصفحة الأولى التي يراها الزائر.

```json
{
  "layout": "layout",
  "widgets": [
    { "name": "header-slider", "widget": "hero-slider" },
    { "name": "categories", "widget": "collection-grid" },
    { "name": "new-products", "widget": "product-grid" },
    { "name": "testimonials", "widget": "testimonials" }
  ]
}
```

### `product.json` - صفحة المنتج

تعرض تفاصيل منتج واحد. المتغير `product` متاح تلقائياً.

```json
{
  "layout": "layout",
  "widgets": [
    { "name": "product-details", "widget": "product-detail" },
    { "name": "related", "widget": "related-products" }
  ]
}
```

### `collection.json` - صفحة التصنيف

تعرض منتجات تصنيف معين. المتغير `collection` متاح تلقائياً.

```json
{
  "layout": "layout",
  "widgets": [
    { "name": "collection-header", "widget": "collection-banner" },
    { "name": "products", "widget": "product-grid" },
    { "name": "pagination", "widget": "pagination" }
  ]
}
```

### `cart.json` - سلة التسوق

```json
{
  "layout": "layout",
  "widgets": [
    { "name": "cart-items", "widget": "cart-list" },
    { "name": "cart-summary", "widget": "cart-total" }
  ]
}
```

### `search.json` - نتائج البحث

المتغير `search` يحتوي على نتائج البحث.

```json
{
  "layout": "layout",
  "widgets": [
    { "name": "search-results", "widget": "search-results" }
  ]
}
```

---

## استخدام تخطيطات مختلفة

يمكن تحديد تخطيط مختلف لكل صفحة:

```json
{
  "layout": "checkout",
  "widgets": []
}
```

يتطلب وجود ملف `layouts/checkout.njk`.

---

## ويدجت مع Blocks

```json
{
  "layout": "layout",
  "widgets": [
    {
      "name": "الأسئلة الشائعة",
      "widget": "faq-accordion",
      "settings": {
        "title": "الأسئلة الشائعة"
      },
      "blocks": [
        {
          "block": "question",
          "settings": {
            "question": "ما هي طرق الدفع؟",
            "answer": "نقبل الدفع عند الاستلام وبطاقات الائتمان."
          }
        },
        {
          "block": "question",
          "settings": {
            "question": "كم يستغرق التوصيل؟",
            "answer": "2-5 أيام عمل."
          }
        }
      ]
    }
  ]
}
```

> ⚠️ **مهم**:
> - استخدم `"block"` وليس `"type"` لتحديد نوع الـ Block
> - كل block يجب أن يكون له `"name"` يظهر في Theme Editor
